# NSL-KDD Dataset Directory

Place your NSL-KDD dataset CSV files inside this folder:
- `KDDTrain+.txt` (or `KDDTrain+.csv`)
- `KDDTest+.txt` (or `KDDTest+.csv`)

Training and threat hunting require both real raw files. The API returns a validation error until they are present; it does not synthesize records.